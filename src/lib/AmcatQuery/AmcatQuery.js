// implement the query filters
// make each query field a separate component (query string + each field type)
// each component takes setQuery as prop, so the design is that the parent keeps the query state
// with query being an object in which each component only updates it's own field
